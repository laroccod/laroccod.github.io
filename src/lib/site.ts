/** Canonical origin for the deployed site. Everything that needs an absolute
 * URL (metadataBase, robots, sitemap, OG cards) reads it from here, so moving
 * the site to a custom domain is a one-line change. No trailing slash. */
export const SITE_URL = "https://laroccod.github.io";
